# Runs everywhere: development, CI and production. Pure-Python only, so a
# fresh clone installs with no compiler and no platform surprises.
asgiref==3.12.1
celery==5.6.3
django-cors-headers==4.9.0
Django==5.2.17
djangorestframework==3.18.0
reportlab==5.0.1
sqlparse==0.6.0
tzdata==2026.3
# Serves Django's own static files (the admin's CSS, chiefly) from the web
# process, so a deployment does not need a separate web server in front just
# to make /admin look like itself. The React bundle is not served from here.
whitenoise==6.11.0
