# Development and CI only. Never installed in production.
#
#     pip install -r requirements-dev.txt
#
# Kept apart from requirements.txt for the same reason requirements-prod.txt
# is: a fresh clone should be able to run the application and its tests
# without the linter, and the deployed image should not carry it at all.
-r requirements.txt

# The linter, the import sorter and the formatter, in one binary. Pinned
# exactly: ruff adds rules in minor releases, and a CI run that fails on code
# nobody touched -- because the runner happened to resolve a newer ruff than
# the machine the change was written on -- teaches people to distrust CI.
# Dependabot proposes the bumps; see .github/dependabot.yml.
ruff==0.14.5
